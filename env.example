# ─────────────────────────────────────────────
# Copy this file to .env.local
# NEVER commit .env.local to Git
# Add all these same keys in Vercel → Project Settings → Environment Variables
# ─────────────────────────────────────────────

# MongoDB Atlas connection string
# Format: mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://YOUR_USER:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/?retryWrites=true&w=majority

# MongoDB database name (will be auto-created if it doesn't exist)
MONGODB_DB_NAME=portfolio

# Gmail address you want to send emails from
GMAIL_USER=your.email@gmail.com
# Gmail address you want to receive emails
GMAIL_RECEIVE=portfolio@email.here

# Gmail App Password (NOT your normal Gmail password)
# How to get it:
#   1. Go to myaccount.google.com → Security
#   2. Enable 2-Step Verification (required)
#   3. Search "App passwords" → Create one → Name it "Portfolio"
#   4. Copy the 16-character password here (no spaces)
GMAIL_PASS=xxxx xxxx xxxx xxxx


# ipinfo.io token for geo lookups (optional but recommended — free tier: 50k/month)
# Sign up at https://ipinfo.io/signup → copy your token from dashboard
IPINFO_TOKEN=your_ipinfo_token_here
